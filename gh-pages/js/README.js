// ele = element
// eles = elemets
// id = name of node
// selector = jquery/cytoscape selector of node/edges
// unit = node + leaves/end-nodes
// nb = neighborhood

// trace/untrace = similar to selection/deselection but also maintaining an array for storing what has been selected
// selection/deselecton = means coloring or highlighting of nodes(brown) or edges(black)
// Clicked Node = just clicked node
// traced end node = node at the end of tracer mechanism | last node in chain of tracer mechanism
// end nodes = leaves of the node in context
// selection state = if node is ".selected" or if the edge is one of the  ".highlighted-edges"
// last node = node clicked previous to node just clicked

// comment terminology: more the number of '#', higher is the level of comment